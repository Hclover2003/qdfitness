import 'package:flutter/material.dart';
import 'package:qdfitness/screens/home/logfood.dart';

class LogFoodWrapper extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: LogFood(),
    );
  }
}
